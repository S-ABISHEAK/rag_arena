VITE_API_URL=http://localhost:8000

# Only needed if the backend's API_KEY is set (see ../.env.example) — leave
# empty for local dev. This file is tracked in git, so never put a real
# deployment key here; set it in your hosting platform's env var UI instead,
# scoped to the production build only.
VITE_API_KEY=
